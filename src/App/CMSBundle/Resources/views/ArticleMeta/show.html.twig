{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ArticleMeta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Metakey</th>
                <td>{{ entity.metaKey }}</td>
            </tr>
            <tr>
                <th>Metavalue</th>
                <td>{{ entity.metaValue }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('articlemeta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('articlemeta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
