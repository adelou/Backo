{% extends 'AppAdminBundle:Layout:admin.html.twig' %}
{% form_theme form 'AppAdminBundle:Form:fields.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    Ajouter un utilisateur
                </header>
                {{ form(form) }}
            </section>
        </div>
    </div>
    <div class="col-md-12">
        <a href="{{ path('user_list') }}" class="btn btn-shadow btn-default"><i class="fa fa-arrow-left"></i> Retour</a>
        <button type="submit" class="btn btn-shadow btn-success pull-right"><i class="fa fa-save"></i> Créer</button>
    </div>
{% endblock %}